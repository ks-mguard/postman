$kind: http-request
name: api/notice/list
url: "{{svr-https-admin-api}}/api/notice/list"
method: POST
headers:
  - key: apiToken
    value: "{{refreshGuardianAccessToken}}"
    disabled: true
body:
  type: json
  content: "{}"
auth:
  type: noauth
settings: {}
order: 3000
