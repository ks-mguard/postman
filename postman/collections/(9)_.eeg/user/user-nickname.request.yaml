$kind: http-request
name: user/nickname
url: "{{url-api}}/api/v1/user/nickname"
method: PATCH
body:
  type: json
  content: |-
    {
        "nickname": "닉네임3"
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
settings: {}
order: 3000
